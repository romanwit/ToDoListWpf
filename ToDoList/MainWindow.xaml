<Window x:Class="ToDoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:conv="clr-namespace:ToDoList.Presentation.Converters"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Task Manager" Height="500" Width="700"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <conv:NullToBoolConverter x:Key="NullToBoolConverter" />
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Filter: " VerticalAlignment="Center" FontWeight="Bold"/>
            <ComboBox Width="150" Margin="5,0,0,0" SelectedValue="{Binding Filter}" SelectedValuePath="Content">
                <ComboBoxItem Content="All" />
                <ComboBoxItem Content="Completed" />
                <ComboBoxItem Content="Not Completed" />
            </ComboBox>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding FilteredTasks}" AutoGenerateColumns="False" CanUserAddRows="False"
                  SelectionMode="Single" SelectedItem="{Binding SelectedTask}" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="*"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Due Date" Binding="{Binding DueDateFormatted}" Width="120"/>
                <DataGridTextColumn Header="CreatedAt" Binding="{Binding CreatedAtFormatted}" Width="*"/>
                <DataGridCheckBoxColumn Header="Completed" Binding="{Binding IsCompleted}" Width="*"/>
                <DataGridTextColumn Header="Priority" Binding="{Binding PriorityText}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0" >
            <Button Content="Add" Width="80" Margin="0,0,5,0" Command="{Binding AddTaskCommand}" />
            <Button Content="Edit" Width="80" Margin="0,0,5,0" Command="{Binding EditTaskCommand}" 
                    IsEnabled="{Binding SelectedTask, Converter={StaticResource NullToBoolConverter}}" />
            <Button Content="Delete" Width="80" Margin="0,0,15,0" Command="{Binding DeleteTaskCommand}"
                    IsEnabled="{Binding SelectedTask, Converter={StaticResource NullToBoolConverter}}" />
            <Button Content="Export to PDF" Width="120" Command="{Binding ExportPdfCommand}" />
        </StackPanel>
    </Grid>
</Window>
